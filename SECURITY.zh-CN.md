# 安全政策

**中文** | [English](SECURITY.md)

Tokenhush 的存在就是为了让密钥不进入 AI 编程工具的请求，所以安全报告值得认真对待。本政策覆盖位于 `github.com/fregie/tokenhush` 的公开 Apache-2.0 内核仓库。本政策仅以英文维护。

[docs/security.zh-CN.md](docs/security.zh-CN.md) 是面向运维者的安全契约：八条不变量、响应阶段的效应、SSE 限制，以及完整的残余风险清单。本文档讲如何报告问题，以及之后会发生什么。

## 威胁模型简述

Tokenhush 是本地、仅回环的 HTTP 网关，位于你的 AI 编程工具与厂商 API 之间。它把出站请求 body 里检测到的密钥替换成会话级占位符，转发清理后的请求，并在响应中还原原始值。模型只看得到占位符，而占位符在出站方向永不回填：只有你的客户端会收到原始值。

它守护什么：

- **仅回环。** 监听器始终绑定 `127.0.0.1`，主机有 IPv6 回环时再加 `[::1]`。非回环绑定在构造上不可能。
- **Host 与 Origin 校验。** Host 白名单始终强制，Origin 对浏览器风格请求做校验。
- **极小的控制 API。** 控制面恰好是 `GET /status`，位于每次 `run` 生成的 bearer token 之后，token 以 `0600` 权限存放。缺失凭据返回 `401`，错误凭据返回 `403`。非 GET 请求返回 JSON `405` 并带 `Allow: GET`，其他 GET 返回 JSON `404`。没有第二个端点，也没有 allowlist 变更端点。
- **什么都不持久化。** 请求或响应 body、检测到的密钥、占位符到密钥的映射都不写盘。只有元数据：会话文件、已验证的规则缓存与防回滚标记。脱敏日志经过掩码、只写 stderr，且永不持久化。
- **故障关闭，而非故障开放。** 检测器失败会以带标签的原因拒绝请求，而不是未脱敏地转发。携带非 identity `Content-Encoding` 的请求以 `415` 拒绝，而不是为检测而解码。
- **无 MITM，无根证书。** Tokenhush 不终止 TLS，也不安装证书。absence 守卫扫描生产 Go 源码中的任何此类路径，一旦出现即让构建失败。
- **恰好两个厂商绑定的出口类别。** 两者都发往 `updates.tokenhush.com`，按命令限定，且可开关：update-check（`TOKENHUSH_NO_UPDATE_CHECK=1`）与 rule-sync（`TOKENHUSH_NO_RULE_SYNC=1`），保留 30 天。除此之外，离开你机器的唯一流量就是你发往自己厂商的请求。

## 残余风险

这些是设计的已知边界，完整记录于 [docs/security.zh-CN.md](docs/security.zh-CN.md)。它们不是秘密，关于它们的报告也欢迎，但就其本身而言，它们是已知局限而不是漏洞。

- **R1：编码后的密钥不会被捕获。** base64、hex 或 URL 编码后才离开的密钥不会被检测。设计上不设 normalization 传递，且非 identity 的请求 `Content-Encoding` 以 `415` 拒绝，而不是为检测而解码。
- **R2：JSON 对象键中的密钥不会被捕获。** 放在 JSON 对象键而不是值里的密钥会原样转发。键位置阻断被删除，因此不存在半可用的键路径。
- **R3：签名远程包可能通过自身 allowlist 弱化检测。** 非弱化下限对 allowlist 保持中立，因此签名包可以通过自身的全局或每条规则的 allowlist 压制匹配。规则签名密钥 `rules-2026-09` 是信任根，一个弱化检测的包是持有该密钥者选择发布的包。
- **R4：OD-2 command 规则拒绝处于休眠，v2 manifest 升级不向后兼容。** 拒绝保持休眠，直到闸门打开；闸门只在规则 manifest `schema_version` 达到 2 时打开。该升级会破坏钉住 `== 1` 的客户端，因此后端必须继续提供 v1 manifest，直到旧版本线退出支持。

完整清单包含七项风险，R1 到 R7，包括响应路径预算限制与 SSE 拆分信封形态。见 [docs/security.zh-CN.md](docs/security.zh-CN.md)。

## 报告漏洞

**不要为安全问题开公开 issue 或 pull request。**

通过 GitHub 的私密漏洞报告私下报告：

**https://github.com/fregie/tokenhush/security/advisories/new**

该表单会开一份私密安全公告，只有你和维护者可见。如果表单不可用，请用维护者 GitHub 主页上的联系方式，并说明你在报告漏洞。

一份有用的报告包括：

- 问题是什么，以及你认为它有什么影响。
- 受影响的版本、commit 或 release。
- 复现步骤，尽可能附上最小配置或请求。
- 任何 PoC 或日志，并先移除密钥。

## 范围

范围内：

- 网关及其数据路径：回环绑定、Host 与 Origin 校验，以及控制 API。
- CLI 与配置解析。
- 规则同步与自更新校验。
- 脱敏正确性：漏检的密钥、改变行为的过度脱敏，以及还原正确性。
- 已发布文档中对某项安全属性描述错误之处。

范围外：

- 厂商后端。请通过厂商自己的渠道报告。
- 第三方工具。请报告给它们自己的项目。
- 上述已记录的残余风险，当报告只是重述它们而没有具体、可利用的案例时。
- 需要本机账户已被攻陷才成立的加固缺失。
- 系统级 MITM 的缺失。公开内核设计上不实现它，所以这不是漏洞。
- 没有影响分析的原始扫描器输出。

不要把私密或客户数据粘贴进报告。

## 会有什么预期

报告按尽力而为处理。这是个小组件项目，不是有专职团队的安全团队。

- 我们的目标是在 **5 个工作日**内确认收到报告。
- 我们的目标是在 **10 个工作日**内给出初步评估，附修复计划或拒绝理由。
- 状态变化时我们会同步你，并在发布任何内容前与你商定披露日期。
- 细节只在某个 release 包含修复后才公开。除非报告者要求匿名，我们会致谢。

如果超过上述时限没收到回复，在同一份私密公告下礼貌追问即可。

## 支持的版本

安全修复落在 `main` 上，随下一个 release 发布。1.0 之前只有最新的 release 线接收安全修复；如果你跑的是更旧的构建，先升级再报告，除非报告本身就是关于升级路径的。

当前线是 `main` 上的 `v0.5.0` 重写。更旧的线，`v0.4.x` 及更早，不受支持。

## 问题

任何非漏洞的问题，开一个普通 issue。设计问题见 [docs/security.zh-CN.md](docs/security.zh-CN.md) 与 [CONTRIBUTING.zh-CN.md](CONTRIBUTING.zh-CN.md)。
