# tokenhush

> **PLACEHOLDER — not the real README.**
>
> This file exists only so the repository bootstrap (todo W0.1) has a README
> path. The real documentation lands with the docs todo **W6.5** and will
> replace this file entirely. Do not rely on anything below this banner.

`tokenhush` is being rewritten from scratch as v0.5.0. Until W6.5 lands, the
authoritative documentation lives in the rewrite plan under `.omo/` (local
only, never committed).
